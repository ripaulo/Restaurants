<p style="color: green"><%= notice %></p>

<h1>Restaurants</h1>

<div id="restaurants">
  <% @restaurants.each do |restaurant| %>
    <%= render restaurant %>
    <p>
      <%= link_to "Show this restaurant", restaurant %>
    </p>
    <p>
      <%= link_to "Show the restaurants on Top", top_restaurants_path%>
    </p>
  <% end %>
</div>

<%= link_to "New restaurant", new_restaurant_path %>
